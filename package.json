{
  "name": "@openclaw/company",
  "version": "0.1.0",
  "description": "一人公司 OS — CEO + AI employee agents for OpenClaw",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "tsdown --entry index.ts --platform node --target node22 --format esm --out-dir . --no-dts --no-clean --external openclaw --external 'node:*'",
    "dev": "bash dev.sh",
    "dev:no-open": "bash dev.sh --no-open",
    "dev:rebuild": "bash dev.sh --rebuild"
  },
  "dependencies": {
    "openclaw": ">=2026.3.14"
  },
  "devDependencies": {
    "@types/node": ">=22",
    "tsdown": "^0.12.9",
    "typescript": "^5.9.3"
  },
  "openclaw": {
    "extensions": [
      "./index.js"
    ]
  }
}
